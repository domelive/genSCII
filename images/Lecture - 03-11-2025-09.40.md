---
date: 2025-11-03 09.40
course: 02 Courses/Anno III/Ingegneria Del Software/Lectures
tags:
  - lecture
  - python
reviewed: false
last_reviewed:
next_review:
---
## 🧠 Concetti
---
#### Liste
+ Collezione ordinata di elementi con duplicati.
+ _Referential Transparency_: il flusso di controllo non influenza l'operazione.
	+ Posso cambiare l'ordine delle operazioni e il risultato non cambia.
	+ Una conseguenza di questo, è che le variabili non cambiano valore.
		+ Lisp: se una variabile ha un solo valore non ho side effects.
	+ Viene ereditato da python attraverso il concetto delle entità immutabili.
		+ Gran parte degli oggetti, una volta che sono istanziati non possono essere mutati.
			+ Quando si assegna ad una variabile una data stringa, ad esempio, la stringa è immutabile ma la variabile può fare riferimento ad un altra stringa.
+ Le liste sono oggetti mutabili.
+ Shallow copy: condivido il puntatore delle lista ad un altra variabile.
#### Tuple
+ Collezione ordinata e immutabile con sintassi tipo array.
	+ Non posso modificare gli elementi.
		+ Ottengo un errore se lo faccio.
	+ Anche se li aggiungo.
+ Se ho una lista dentro una tupla posso ovviamente modificarla.
	+ Cambio la lista ma non la tupla.
	+ Al posto della lista posso avere qualsiasi altra struttura mutabile.
+ Posso creare una concatenazione di tuple con l'operatore `+`.
	+ Solo alla creazione di una nuova tupla.
+ Esiste la deep copy.
	+ Copia tutti gli elementi fino in fondo.
#### Insiemi
+ Collezioni disordinate di elementi senza duplicati, i cui elementi sono immutabili.
+ L'operatore di concatenazione non è supportato per gli insiemi in quanto è definita l'unione.
+ Non posso inserire elementi mutabili all'interno di un insieme.
#### Dizionari
+ Collezioni ordinate di coppia chiave-valore.
+ Ogni chiave può apparire solamente una volta.
#### Iterator
+ Classe che supporta iterazioni su collezioni.
	+ Funzione `__next__()` che ritorna l'elemento successivo.
		+ Ogni volta si modifica lo stato interno dell'iterator in modo che di volta in volta venga ritornato il giusto elemento successivo.
	+ Solleva un'eccezione quando non ci sono più elementi.
+ Ad esempio quando iteriamo su una stringa si crea un iterator.
	+ È un `iterable`.
		+ Ha un metodo chiamato `iter()` che ritorna un iterator.
+ Il `for` itera su un iterator.
	+ Ad ogni iterazione avviene una chiamata al metodo `__next__()` per andare all'iterazione successiva.

## ❓ Domande
---

## 💡 Riferimenti
---

## 🧩 Tasks
---
+ [ ] Review [[Lecture - 03-11-2025-09.40]]

```button 
name ✅ Mark [[Lecture - 03-11-2025-09.40]] As Reviewed 
type command 
action QuickAdd: Mark As Reviewed
```
